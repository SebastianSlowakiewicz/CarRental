<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
    <title>Wypożyczalnia samochodów</title>
</head>
<body>
    <header class="container sticky-bottom text-center">
        <h1>Wypożyczalnia samochodów</h1>
        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid">
                <div class="navbar-nav">
                    <a class="nav-link" href="{% url 'index' %}">Strona główna</a>
                    <a class="nav-link" href="{% url 'cars' %}">Samochody</a>
                    <a class="nav-link" href="{% url 'categories' %}">Kategorie</a>
                </div>
                <div class="navbar-nav">
                    {% if user.is_authenticated %}
                    <a class="nav-link" href="{% url 'dashboard' %}">{{ user.username }}</a>
                    {% else %}
                    <a class="nav-link" href="{% url 'login' %}">Zaloguj</a>
                    <a class="nav-link" href="{% url 'register' %}">Zarejestruj</a>
                    {% endif %}
                    {% if user.is_authenticated %}
                    <a class="btn btn-outline-success" href="{% url 'logout' %}">Wyloguj</a>
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>
    <section  class="container">
        <div class="row">
            <div class="col-8">
                {% block left_panel %}
                
                {% endblock left_panel %}
            </div>
            <div class="col-4">
                {% block right_panel %}
                
                {% endblock right_panel %}
            </div>
        </div>
    </section>
    <footer class="container-fluid alert alert-success text-center sticky-bottom">&copy; Sebastian Słowakiewicz | Akademia Nauk Stosowanych w Nowym Targu</footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js" integrity="sha384-k6d4wzSIapyDyv1kpU366/PK5hCdSbCRGRCMv+eplOQJWyd1fbcAu9OCUj5zNLiq" crossorigin="anonymous"></script>
</body>
</html>